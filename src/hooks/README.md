# hooks

Global, reusable hooks with no business logic, e.g. `useWebSocket`,
`useDebounce`, `useMediaQuery`, `useLocalStorage`, `useInterval`.

Deferred to a later milestone. Feature-specific data hooks live inside each
feature (`features/*/hooks`), not here.
