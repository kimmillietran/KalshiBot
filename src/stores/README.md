# stores

Global, ephemeral client UI state (Zustand): selected market, watchlist filters,
sidebar/layout state, theme.

Deferred to a later milestone. Server/remote state belongs in TanStack Query,
not here. Feature-scoped stores may live inside a feature instead.
