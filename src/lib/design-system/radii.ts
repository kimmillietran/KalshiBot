/** Glass / dashboard panel outer radius — matches --radius-panel. */
export const radiusPanel = "rounded-panel";

/** Inset stat card radius — matches --radius-card. */
export const radiusCard = "rounded-card";

/** Badge / pill radius — matches --radius-badge. */
export const radiusBadge = "rounded-badge";

/** Full round (progress bars, dots). */
export const radiusFull = "rounded-full";
