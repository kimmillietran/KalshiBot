/** Panel body padding — matches --space-panel (1rem). */
export const panelPadding = "p-4";

/** Panel header padding — matches --space-panel-header-* */
export const panelHeaderPadding = "px-4 py-3";

/** Gap between sections inside a panel. */
export const panelGap = "gap-4";

/** Gap between dashboard grid cells. */
export const gridGap = "gap-4";

/** Gap between stat/metric cards in a row. */
export const statGap = "gap-2";

/** Compact stat card internal padding. */
export const statCardPadding = "px-3 py-2.5";

/** Bottom padding for dashboard scroll end. */
export const dashboardBottomPadding = "pb-6";

/** Vertical rhythm between dashboard sections. */
export const dashboardSectionGap = "space-y-4";
