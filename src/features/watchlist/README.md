# Feature: watchlist

User-curated tracking of Kalshi BTC markets.

Deferred to a later milestone.

Interacts with: `market-data` (market refs), `lib/db` (persistence), `stores`
(client-side selection). Consumed by the watchlist route and dashboard.
