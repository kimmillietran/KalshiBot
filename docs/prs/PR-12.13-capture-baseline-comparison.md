# PR-12.13 — Post-Capture Baseline Comparison Report

## Summary

Compares pre-M12.8 configured baseline metrics against post-capture research artifacts and optional run selections.

## CLI

```bash
npm run research:capture-baseline-comparison -- --latest
```

Outputs:

- `data/research-results/capture-baseline-comparison.json`
- `data/reports/capture-baseline-comparison.html`

## Quality gates

```bash
npm run lint
npm run build
npx vitest run src/lib/data/research/captureBaselineComparison
npx tsx scripts/research/buildCaptureBaselineComparison.ts --latest
```
